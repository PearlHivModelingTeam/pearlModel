name: myenv
channels:
  - conda-forge
dependencies:
  - python=3.11.*
  - bioconda::snakemake
  - dask
  - scipy
  - pytables
  - pip
  - pyright
  - ruff
  - mypy
  - sphinx
  - pre-commit
  - pip:
    - pip
    - setuptools
    - wheel
prefix: /opt/conda/envs/myenv
